# minimal template.  uses no job or pipeline explicitely defined.
name: simple.minimal $(Rev:r)
trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

steps:
- script: echo minimal one line
  displayName: one-line script

- script: |
    echo minimal multiline 01
    echo minimal multiline 01
  displayName: multi-line script