# fail task by passing params to generic task command
parameters:
   reason: 'no reason specified'

steps:
- template: step.task.complete.yml
  parameters:
     result: Failed
     message: ${{ parameters.reason }}